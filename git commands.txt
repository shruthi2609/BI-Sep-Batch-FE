GIT

local repository
synchronize the changes to the remote repository



Github - web application 

remote repository - cloud

features

GIT commands:

1.setup git in the folder 

git init

local to remote repository:

local repo -> remote repo - No

local repository -> staging area -> commit changes -> remote

2.git status -> gets the status of the files / to 
check whether its in staging area or not

3.to move the file one by one to staging area:

git add filename.extn

4.all the files to staging area:

git add -A

5.commit my changes

git commit -m "commit message"

6.create a empty remote repo
github ->create repo with the same name as local repo

7.git remote add origin your_remote_repo_link
(one time)

8.git push -u origin branchname(master)












